---
phase: 02-company-search-and-snapshot-preview
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/recipes/page.tsx
  - src/features/company-search/components/company-search-panel.tsx
  - src/features/company-search/components/company-results-table.tsx
  - src/features/company-search/components/company-column-picker.tsx
  - src/features/company-search/lib/company-search-form.ts
  - src/app/recipes/actions.ts
  - src/app/globals.css
autonomous: true
requirements:
  - COMP-01
  - COMP-02
requirements_addressed:
  - COMP-01
  - COMP-02
must_haves:
  truths:
    - The user can submit company search filters from the workspace using controls that match the Apollo filter contract.
    - The user can review company results in a paginated preview table with practical default fields.
    - The user can choose to reveal more Apollo-returned columns without re-querying the search.
  artifacts:
    - src/features/company-search/components/company-search-panel.tsx
    - src/features/company-search/components/company-results-table.tsx
    - src/features/company-search/components/company-column-picker.tsx
    - src/app/recipes/page.tsx
  key_links:
    - Search form controls are generated from or validated against the filter-definition layer from Plan 02-01.
    - The results table reads normalized snapshot data and selectable columns rather than raw Apollo payloads directly.
---

<objective>
Build the company search UI inside the recipe workspace, including validated search controls, paginated company preview, and selectable result columns.

Purpose: Deliver the user-facing Phase 2 workflow while preserving the dense, operator-first interface established in Phase 1.
Output: Search panel, preview table, column picker, and recipe-workspace integration for explicit company search runs.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-search-and-snapshot-preview/02-CONTEXT.md
@.planning/phases/02-company-search-and-snapshot-preview/02-RESEARCH.md
@.planning/phases/02-company-search-and-snapshot-preview/02-01-PLAN.md
@src/app/recipes/page.tsx
@src/features/recipes/components/recipe-editor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add company search controls to the recipe workspace</name>
  <files>src/features/company-search/lib/company-search-form.ts, src/features/company-search/components/company-search-panel.tsx, src/app/recipes/actions.ts, src/app/recipes/page.tsx</files>
  <read_first>src/app/recipes/page.tsx, src/features/recipes/components/recipe-editor.tsx, src/lib/apollo/company-filter-definitions.ts, src/lib/company-search/schema.ts</read_first>
  <action>Create a company search panel that sits alongside the existing recipe workspace and uses Apollo-aligned control types. Enum-like or boolean-like parameters must render constrained controls; open-ended values must render text or multi-value fields. Update server actions so the user can explicitly submit a company search from the active recipe or current draft. Keep the UI dense and operator-focused instead of turning the flow into a wizard.</action>
  <verify>test -f src/features/company-search/components/company-search-panel.tsx && test -f src/features/company-search/lib/company-search-form.ts</verify>
  <acceptance_criteria>
    - company search panel exists and renders in the recipes workspace
    - form control types are driven by the company filter definitions rather than hard-coded ad hoc field choices
    - search submission remains an explicit user-triggered action
  </acceptance_criteria>
  <done>User can submit company search requests from the Phase 1 workspace using Apollo-aligned inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Build paginated preview and selectable columns</name>
  <files>src/features/company-search/components/company-results-table.tsx, src/features/company-search/components/company-column-picker.tsx, src/app/recipes/page.tsx, src/app/globals.css</files>
  <read_first>src/app/recipes/page.tsx, src/app/globals.css, src/lib/db/repositories/company-snapshots.ts</read_first>
  <action>Create a paginated company preview table that shows the default must-have fields: company name, domain, location, employee range, industry, and Apollo company identifier/status. Add a column-picker control that lets the user reveal additional Apollo-returned fields already present in the normalized snapshot data. Keep preview-column changes local to the UI/read model so they do not trigger another Apollo search.</action>
  <verify>test -f src/features/company-search/components/company-results-table.tsx && test -f src/features/company-search/components/company-column-picker.tsx</verify>
  <acceptance_criteria>
    - results table renders the default company preview fields
    - column picker exists and can add optional Apollo-returned fields to the table
    - table pagination state is present for company preview navigation
  </acceptance_criteria>
  <done>The user can inspect company results in a useful preview table and expand visible data without re-querying Apollo.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] Manual smoke check confirms the workspace can render the search form and preview table with optional columns
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The company preview UI is pleasant, dense, and clearly aligned with the Apollo-backed workflow
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-search-and-snapshot-preview/02-02-SUMMARY.md`
</output>
