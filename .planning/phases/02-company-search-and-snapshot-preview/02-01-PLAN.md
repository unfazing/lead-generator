---
phase: 02-company-search-and-snapshot-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/recipes/schema.ts
  - src/lib/apollo/company-search.ts
  - src/lib/apollo/company-filter-definitions.ts
  - src/lib/db/repositories/company-snapshots.ts
  - src/lib/db/schema/company-snapshots.ts
  - src/lib/company-search/schema.ts
  - src/app/api/company-search/route.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-03
user_setup:
  - Set APOLLO_API_KEY to enable live company search verification against Apollo.
must_haves:
  truths:
    - Company search requests are validated against an explicit Apollo-aligned filter contract before they are sent.
    - Company search runs only through a server-owned API path and can persist reusable snapshots.
    - Snapshot storage preserves enough metadata to safely reuse or refresh a search later.
  artifacts:
    - src/lib/apollo/company-filter-definitions.ts
    - src/lib/apollo/company-search.ts
    - src/lib/db/repositories/company-snapshots.ts
    - src/app/api/company-search/route.ts
  key_links:
    - The request validation schema is derived from the filter-definition layer used by the frontend.
    - The company search route writes normalized results to the snapshot repository instead of returning only transient data.
---

<objective>
Create the server-side contract for Phase 2: Apollo-aligned company filter definitions, backend search execution, and durable snapshot persistence.

Purpose: Give the UI a trustworthy backend that matches Apollo’s request semantics and avoids blind repeat credit spend.
Output: Filter definition layer, validated company search route, normalized response model, and company snapshot repository.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-search-and-snapshot-preview/02-CONTEXT.md
@.planning/phases/02-company-search-and-snapshot-preview/02-RESEARCH.md
@src/lib/recipes/schema.ts
@src/lib/db/repositories/recipes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Apollo-aligned company filter metadata and validation</name>
  <files>src/lib/recipes/schema.ts, src/lib/company-search/schema.ts, src/lib/apollo/company-filter-definitions.ts</files>
  <read_first>src/lib/recipes/schema.ts, .planning/phases/02-company-search-and-snapshot-preview/02-CONTEXT.md, .planning/phases/02-company-search-and-snapshot-preview/02-RESEARCH.md</read_first>
  <action>Refactor the recipe/company filter shape so supported company search inputs are represented through a single Apollo-aligned filter-definition layer. Create `src/lib/apollo/company-filter-definitions.ts` with structured metadata for each supported filter, including display label, input control type, serialization key, and any constrained allowed values. Add `src/lib/company-search/schema.ts` with Zod validation for the normalized company search payload derived from those definitions. Update `src/lib/recipes/schema.ts` so company filters match the normalized payload structure instead of the Phase 1 placeholder keyword/location/employee-only shape.</action>
  <verify>test -f src/lib/apollo/company-filter-definitions.ts && test -f src/lib/company-search/schema.ts</verify>
  <acceptance_criteria>
    - src/lib/apollo/company-filter-definitions.ts exports metadata for every supported Phase 2 company filter
    - src/lib/company-search/schema.ts validates a normalized search payload with constrained values where applicable
    - src/lib/recipes/schema.ts uses the Phase 2 company filter shape rather than the original placeholder-only structure
  </acceptance_criteria>
  <done>Apollo-aligned company filter definitions and validation exist as the source of truth for Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend company search execution and snapshot persistence</name>
  <files>src/lib/apollo/company-search.ts, src/lib/db/schema/company-snapshots.ts, src/lib/db/repositories/company-snapshots.ts, src/app/api/company-search/route.ts</files>
  <read_first>src/lib/env.ts, src/lib/db/client.ts, src/lib/db/repositories/recipes.ts, src/lib/company-search/schema.ts</read_first>
  <action>Create a server-only Apollo company search module that accepts the normalized payload, serializes it into Apollo’s request format, and normalizes the response into a preview-friendly row shape. Add a company snapshot schema and repository that stores request signature, fetched timestamp, normalized request payload, returned rows, and warning metadata. Implement `src/app/api/company-search/route.ts` to support explicit search execution and snapshot reuse/refresh semantics through request parameters or body fields that distinguish between using an existing snapshot and fetching the latest snapshot.</action>
  <verify>test -f src/lib/apollo/company-search.ts && test -f src/lib/db/repositories/company-snapshots.ts && test -f src/app/api/company-search/route.ts</verify>
  <acceptance_criteria>
    - src/lib/apollo/company-search.ts performs server-side request normalization and response normalization
    - src/lib/db/repositories/company-snapshots.ts can create, read, and look up snapshots by request signature
    - src/app/api/company-search/route.ts supports explicit execution and explicit latest-snapshot refresh behavior
  </acceptance_criteria>
  <done>Phase 2 has a backend search/snapshot foundation that the preview UI can rely on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] Backend code clearly separates snapshot reuse from latest-refresh execution
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 2 backend contract is strong enough that UI work can be built without guessing at Apollo request semantics
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-search-and-snapshot-preview/02-01-SUMMARY.md`
</output>
