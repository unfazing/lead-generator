---
phase: 02-company-search-and-snapshot-preview
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/app/recipes/page.tsx
  - src/features/company-search/components/company-search-warning.tsx
  - src/features/company-search/lib/company-search-warnings.ts
  - src/lib/db/repositories/company-snapshots.ts
  - src/app/globals.css
autonomous: true
requirements:
  - COMP-03
  - COMP-04
requirements_addressed:
  - COMP-03
  - COMP-04
must_haves:
  truths:
    - Existing company snapshots are reused by default and the user can explicitly request the latest snapshot.
    - Broad searches surface a visible warning but do not hard-block result review.
    - Snapshot metadata is clear enough that the user can tell whether they are looking at reused data or a newly fetched result.
  artifacts:
    - src/features/company-search/lib/company-search-warnings.ts
    - src/features/company-search/components/company-search-warning.tsx
    - src/lib/db/repositories/company-snapshots.ts
    - src/app/recipes/page.tsx
  key_links:
    - Snapshot records include freshness and warning metadata that the workspace displays.
    - The broad-search warning logic is based on snapshot/result metadata rather than generic placeholder text.
---

<objective>
Finish the Phase 2 workflow by making snapshot reuse explicit, latest-refresh behavior clear, and broad-search warnings visible without blocking continuation.

Purpose: Close the biggest usability and credit-safety gaps before Phase 3 depends on company snapshots.
Output: Snapshot freshness UI, explicit latest-refresh action, broad-search warning component, and final Phase 2 workflow polish.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-search-and-snapshot-preview/02-CONTEXT.md
@.planning/phases/02-company-search-and-snapshot-preview/02-RESEARCH.md
@.planning/phases/02-company-search-and-snapshot-preview/02-01-PLAN.md
@.planning/phases/02-company-search-and-snapshot-preview/02-02-PLAN.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add snapshot freshness and latest-refresh behavior</name>
  <files>src/lib/db/repositories/company-snapshots.ts, src/app/recipes/page.tsx</files>
  <read_first>src/lib/db/repositories/company-snapshots.ts, src/app/recipes/page.tsx, .planning/phases/02-company-search-and-snapshot-preview/02-CONTEXT.md</read_first>
  <action>Extend snapshot repository reads and the workspace state so the active company preview clearly indicates whether it is reusing an existing snapshot and when it was fetched. Add an explicit “get latest snapshot” action that fetches fresh Apollo data while preserving the default reuse behavior when a matching snapshot already exists.</action>
  <verify>rg -n \"latest snapshot|fetched\" src/app/recipes/page.tsx src/lib/db/repositories/company-snapshots.ts</verify>
  <acceptance_criteria>
    - workspace shows snapshot freshness information
    - workspace includes a separate explicit latest-refresh action
    - default path still reuses an existing matching snapshot instead of always calling Apollo
  </acceptance_criteria>
  <done>Snapshot reuse and refresh behavior are explicit to the operator.</done>
</task>

<task type="auto">
  <name>Task 2: Surface broad-search warnings without blocking the flow</name>
  <files>src/features/company-search/lib/company-search-warnings.ts, src/features/company-search/components/company-search-warning.tsx, src/app/recipes/page.tsx, src/app/globals.css</files>
  <read_first>.planning/research/PITFALLS.md, src/app/recipes/page.tsx, src/app/globals.css</read_first>
  <action>Create warning logic and UI that detect broad-search risk from available result metadata, pagination state, or count thresholds and display a visible warning in the company search workspace. The warning must explain potential credit waste or truncation risk and still allow the user to continue reviewing results, matching the user’s chosen warning-not-blocking behavior.</action>
  <verify>test -f src/features/company-search/lib/company-search-warnings.ts && test -f src/features/company-search/components/company-search-warning.tsx</verify>
  <acceptance_criteria>
    - broad-search warning component exists and renders in the workspace
    - warning copy explains that the user may continue while still highlighting credit/truncation risk
    - broad-search warnings do not disable or remove result preview access
    - warning and snapshot-freshness states can be exercised without repeated live Apollo searches
  </acceptance_criteria>
  <done>Phase 2 ends with visible, non-blocking guardrails around broad company searches.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] Manual smoke check confirms snapshot reuse, latest refresh, and broad-search warnings behave distinctly while minimizing live credit-bearing calls
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Snapshot reuse and broad-search warnings make the workflow safer without adding friction or hidden API calls
</success_criteria>

<output>
After completion, create `.planning/phases/02-company-search-and-snapshot-preview/02-03-SUMMARY.md`
</output>
