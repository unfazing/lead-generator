---
phase: 01-recipe-and-usage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/env.ts
  - src/lib/db/client.ts
  - src/lib/db/schema/recipes.ts
  - src/lib/recipes/schema.ts
autonomous: true
requirements:
  - RECP-01
  - RECP-02
  - RECP-03
user_setup:
  - Provide Apollo master API key in local env for usage telemetry development.
  - Decide the initial database connection target (`DATABASE_URL`) before runtime verification.
must_haves:
  truths:
    - The app has a running server-backed foundation suitable for a single-user internal tool.
    - A recipe schema exists that can represent company filters, person filters, and export column settings together.
    - Apollo secrets and database configuration are read only on the server.
  artifacts:
    - src/lib/recipes/schema.ts
    - src/lib/db/schema/recipes.ts
    - src/lib/env.ts
    - src/app/layout.tsx
  key_links:
    - Recipe schema types map cleanly to the persisted recipe table definition.
    - Server-only env validation is used by Apollo- and DB-facing modules.
---

<objective>
Create the application foundation for Apollo Lead Finder so later plans can add recipe workflows and usage telemetry without reworking the stack.

Purpose: Establish the project shell, server-only configuration boundaries, and persisted recipe model that Phase 1 depends on.
Output: A bootstrapped Next.js app with shared recipe schema, persistence scaffolding, and validated server env access.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recipe-and-usage-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Next.js application and core tooling</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <read_first>.planning/research/STACK.md, .planning/PROJECT.md</read_first>
  <action>Initialize the project as a Next.js 15 App Router application with React 19, TypeScript, Tailwind CSS, ESLint, and Prettier aligned to the researched stack. Create a minimal app shell that clearly positions the app as a server-backed internal tool and leaves room for a recipe workspace. Prefer a `src/` layout to match the researched project structure.</action>
  <verify>test -f package.json && test -f src/app/layout.tsx && test -f src/app/page.tsx</verify>
  <acceptance_criteria>
    - package.json includes `next`, `react`, `react-dom`, and `typescript`
    - src/app/layout.tsx exists and imports globals.css
    - src/app/page.tsx renders an initial application shell, not a placeholder README page
  </acceptance_criteria>
  <done>Project boots with the intended framework stack and a real application entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add server-only environment and persistence scaffolding</name>
  <files>src/lib/env.ts, src/lib/db/client.ts, src/lib/db/schema/recipes.ts, src/lib/recipes/schema.ts</files>
  <read_first>.planning/research/STACK.md, .planning/research/ARCHITECTURE.md, .planning/phases/01-recipe-and-usage-foundation/01-CONTEXT.md</read_first>
  <action>Create validated server env loading with Zod, a database client wrapper, and an initial recipes table/schema pair. The shared recipe schema must include company filters, person filters, and export column settings together so later phases can reuse it without shape drift. Keep Apollo credentials server-only and do not introduce browser-facing secret access.</action>
  <verify>test -f src/lib/env.ts && test -f src/lib/db/schema/recipes.ts && test -f src/lib/recipes/schema.ts</verify>
  <acceptance_criteria>
    - src/lib/env.ts validates required server env vars with Zod
    - src/lib/recipes/schema.ts defines one recipe shape containing company, people, and export configuration
    - src/lib/db/schema/recipes.ts persists the recipe shape or normalized subfields intentionally
  </acceptance_criteria>
  <done>Recipe persistence and env boundaries exist for the rest of Phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm exec tsc --noEmit` passes
- [ ] App shell and server-only env modules exist with no Apollo secret leakage into client code
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a real implementation foundation for Phase 1 rather than placeholder docs only
</success_criteria>

<output>
After completion, create `.planning/phases/01-recipe-and-usage-foundation/01-01-SUMMARY.md`
</output>
