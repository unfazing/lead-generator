---
phase: 01-recipe-and-usage-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/app/api/apollo/usage/route.ts
  - src/features/usage/components/usage-summary.tsx
  - src/features/usage/lib/apollo-usage.ts
  - src/app/recipes/page.tsx
autonomous: true
requirements:
  - COST-01
must_haves:
  truths:
    - The user can see Apollo usage or credit information in the frontend before starting any search flow.
    - Usage information is presented as an operator-facing summary, not buried in a settings page.
    - Apollo usage fetching happens on the server and respects the project's secret-handling constraints.
  artifacts:
    - src/app/api/apollo/usage/route.ts
    - src/features/usage/lib/apollo-usage.ts
    - src/features/usage/components/usage-summary.tsx
  key_links:
    - The recipe workspace renders the usage summary fed by the server usage endpoint or server component data path.
    - Apollo usage requests use server-only configuration from Plan 01.
---

<objective>
Add Apollo usage visibility to the recipe workspace so credit preservation is visible from the first usable phase.

Purpose: Fulfill the product promise that users can see account usage before spending credits in later phases.
Output: Server-backed Apollo usage fetching and a compact usage summary integrated into the recipe UI.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recipe-and-usage-foundation/01-CONTEXT.md
@.planning/phases/01-recipe-and-usage-foundation/01-01-PLAN.md
@.planning/phases/01-recipe-and-usage-foundation/01-02-PLAN.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement server-backed Apollo usage retrieval</name>
  <files>src/features/usage/lib/apollo-usage.ts, src/app/api/apollo/usage/route.ts</files>
  <read_first>.planning/research/STACK.md, .planning/research/PITFALLS.md, src/lib/env.ts</read_first>
  <action>Create a server-only Apollo usage client and expose a narrow app-facing endpoint or server data path for current usage information. Normalize the response into a UI-friendly shape that emphasizes available headroom, recent consumption, and any obvious limits relevant to the single-user operator flow.</action>
  <verify>test -f src/features/usage/lib/apollo-usage.ts && test -f src/app/api/apollo/usage/route.ts</verify>
  <acceptance_criteria>
    - Apollo usage retrieval lives in server-side code only
    - usage response is normalized for UI rendering instead of exposing raw Apollo payloads directly
    - failure handling returns a usable empty or warning state rather than crashing the page
  </acceptance_criteria>
  <done>Server-backed usage data is available to the frontend safely.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate a compact usage summary into the recipe workspace</name>
  <files>src/features/usage/components/usage-summary.tsx, src/app/recipes/page.tsx</files>
  <read_first>.planning/phases/01-recipe-and-usage-foundation/01-CONTEXT.md, src/app/api/apollo/usage/route.ts</read_first>
  <action>Add a compact, easy-to-scan usage summary to the recipe workspace so the user sees Apollo credit/account state before any future company search action. Keep the presentation practical and operator-focused. Do not turn this into a billing dashboard or settings-only surface.</action>
  <verify>test -f src/features/usage/components/usage-summary.tsx</verify>
  <acceptance_criteria>
    - the recipe workspace visibly includes Apollo usage information near the top-level working surface
    - usage summary communicates current usage/headroom in compact copy or stat blocks
    - usage failure state is visible but non-blocking to recipe editing
  </acceptance_criteria>
  <done>Phase 1 delivers visible Apollo usage telemetry in the working UI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm exec tsc --noEmit` passes
- [ ] Manual smoke check confirms the recipe workspace shows usage data or a graceful warning state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Apollo usage visibility is present in the main workspace before any search functionality exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-recipe-and-usage-foundation/01-03-SUMMARY.md`
</output>
