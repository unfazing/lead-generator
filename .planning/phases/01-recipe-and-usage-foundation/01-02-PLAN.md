---
phase: 01-recipe-and-usage-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/page.tsx
  - src/app/recipes/actions.ts
  - src/app/recipes/page.tsx
  - src/features/recipes/components/recipe-list.tsx
  - src/features/recipes/components/recipe-editor.tsx
  - src/features/recipes/lib/recipe-form.ts
  - src/lib/db/repositories/recipes.ts
autonomous: true
requirements:
  - RECP-01
  - RECP-02
  - RECP-03
must_haves:
  truths:
    - The user can create a recipe that combines company filters, person filters, and export settings.
    - Saved recipes are persisted and visible when the app is reopened.
    - Recipe editing is explicit and deliberate rather than hidden behind autosave.
  artifacts:
    - src/app/recipes/page.tsx
    - src/features/recipes/components/recipe-editor.tsx
    - src/features/recipes/components/recipe-list.tsx
    - src/app/recipes/actions.ts
  key_links:
    - Recipe form validation matches the shared recipe schema from Plan 01.
    - Recipe save/load actions use the repository layer instead of client-only state.
---

<objective>
Implement the first usable recipe workspace so the user can create, edit, and persist reusable prospecting recipes.

Purpose: Deliver the main Phase 1 workflow surface and prove the recipe model is practical before search work begins.
Output: A recipe workspace with list/detail editing, persistence hooks, and explicit save/update actions.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recipe-and-usage-foundation/01-CONTEXT.md
@.planning/phases/01-recipe-and-usage-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the persisted recipe repository and actions</name>
  <files>src/lib/db/repositories/recipes.ts, src/app/recipes/actions.ts</files>
  <read_first>src/lib/db/schema/recipes.ts, src/lib/recipes/schema.ts</read_first>
  <action>Create a recipe repository and server actions or route handlers for listing, creating, and updating recipes. Keep actions server-owned and validate all inputs against the shared recipe schema before persistence. Return response shapes that support a simple internal operator UI.</action>
  <verify>test -f src/lib/db/repositories/recipes.ts && test -f src/app/recipes/actions.ts</verify>
  <acceptance_criteria>
    - recipe repository exposes create, update, and list capabilities
    - recipe write paths validate inputs against the shared schema
    - no recipe persistence depends on localStorage as the source of truth
  </acceptance_criteria>
  <done>Recipe data can be created, updated, and read back from durable storage.</done>
</task>

<task type="auto">
  <name>Task 2: Build the recipe list and editor workspace</name>
  <files>src/app/page.tsx, src/app/recipes/page.tsx, src/features/recipes/components/recipe-list.tsx, src/features/recipes/components/recipe-editor.tsx, src/features/recipes/lib/recipe-form.ts</files>
  <read_first>.planning/phases/01-recipe-and-usage-foundation/01-CONTEXT.md, src/app/recipes/actions.ts</read_first>
  <action>Create a dense internal-tool recipe workspace with a list of saved recipes and an editor form for company filters, person filters, and export column settings. Use explicit save/update actions. Preserve the single-user assumption and do not introduce auth or collaboration UI. Make the main route point into this workspace so Phase 1 has an obvious home screen.</action>
  <verify>test -f src/features/recipes/components/recipe-editor.tsx && test -f src/features/recipes/components/recipe-list.tsx</verify>
  <acceptance_criteria>
    - recipe editor exposes sections for company filters, person filters, and export settings
    - recipe list renders saved recipes from durable storage
    - the UI includes an explicit save or update action rather than implicit autosave
  </acceptance_criteria>
  <done>User-facing recipe management works from the frontend.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm exec tsc --noEmit` passes
- [ ] Manual smoke check confirms recipes can be created and loaded after a refresh
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Recipe creation and editing are durable and practical enough to support later search flows
</success_criteria>

<output>
After completion, create `.planning/phases/01-recipe-and-usage-foundation/01-02-SUMMARY.md`
</output>
