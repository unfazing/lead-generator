---
phase: 03-people-discovery-and-run-planning
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/search/page.tsx
  - src/features/people-search/components/people-results-table.tsx
  - src/features/run-planning/components/run-plan-panel.tsx
  - src/features/run-planning/lib/run-plan-estimates.ts
  - src/lib/db/schema/run-plans.ts
  - src/lib/db/repositories/run-plans.ts
  - src/app/globals.css
autonomous: true
requirements:
  - COST-02
  - COST-03
requirements_addressed:
  - COST-02
  - COST-03
must_haves:
  truths:
    - The user can see a retrieval estimate before any verified-email retrieval can start.
    - The user can define explicit stop conditions in the plan step, including a maximum contacts cap.
    - The estimate and stop conditions are tied to the reviewed people snapshot rather than free-floating form state.
  artifacts:
    - src/features/run-planning/components/run-plan-panel.tsx
    - src/features/run-planning/lib/run-plan-estimates.ts
    - src/lib/db/repositories/run-plans.ts
    - src/app/search/page.tsx
  key_links:
    - The planner reads persisted people snapshot counts and selected filters to produce the estimate.
    - Stop-condition inputs are stored with the plan record that later gates retrieval kickoff.
---

<objective>
Build the explicit run planner for Phase 3: estimate visibility, stop-condition inputs, and persisted pre-retrieval plan records.

Purpose: Give the user a visible, reviewable planning step between people preview and later verified-email retrieval.
Output: Run planning panel, estimate logic, plan persistence, and search-page integration.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-people-discovery-and-run-planning/03-CONTEXT.md
@.planning/phases/03-people-discovery-and-run-planning/03-01-PLAN.md
@src/app/search/page.tsx
@src/features/usage/components/usage-summary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement estimate calculation and persisted run plans</name>
  <files>src/features/run-planning/lib/run-plan-estimates.ts, src/lib/db/schema/run-plans.ts, src/lib/db/repositories/run-plans.ts</files>
  <read_first>.planning/research/SUMMARY.md, .planning/research/PITFALLS.md, src/lib/db/repositories/company-snapshots.ts, src/lib/db/repositories/people-snapshots.ts</read_first>
  <action>Create a lightweight estimate module that derives a conservative retrieval estimate from the reviewed people snapshot and the active filters. Add a run-plan schema and repository that persist source people snapshot, estimate details, and stop conditions such as maximum contacts. The estimate does not need to be perfect, but it must be explicit and clearly tied to the reviewed snapshot state.</action>
  <verify>test -f src/features/run-planning/lib/run-plan-estimates.ts && test -f src/lib/db/repositories/run-plans.ts</verify>
  <acceptance_criteria>
    - estimate logic exists and produces visible structured output
    - run plans persist stop conditions and source snapshot references
    - estimate language makes uncertainty clear instead of implying exact billing
  </acceptance_criteria>
  <done>Run planning has durable estimate and stop-condition data behind it.</done>
</task>

<task type="auto">
  <name>Task 2: Add run planner UI to the search page</name>
  <files>src/features/run-planning/components/run-plan-panel.tsx, src/app/search/page.tsx, src/app/globals.css</files>
  <read_first>src/app/search/page.tsx, src/app/globals.css, src/features/company-search/components/company-results-workspace.tsx</read_first>
  <action>Create a run-planning panel on `/search` that becomes available after a reviewed people snapshot exists. Show the estimate, stop-condition inputs, and enough source context that the user knows which recipe pairing and people snapshot the plan refers to. Keep the interface dense and operational rather than modal-heavy.</action>
  <verify>test -f src/features/run-planning/components/run-plan-panel.tsx</verify>
  <acceptance_criteria>
    - planner panel renders with estimate and stop conditions on the search page
    - planner is clearly tied to the active people snapshot and recipe pairing
    - stop conditions include at least a maximum contacts cap
    - planner UI does not initiate retrieval execution yet
  </acceptance_criteria>
  <done>The user can prepare a retrieval plan with visible estimate and limits.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] Manual smoke check confirms the planner is unavailable before people preview exists and available after people snapshot review
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The user can review an estimate and stop conditions before retrieval is even offered
</success_criteria>

<output>
After completion, create `.planning/phases/03-people-discovery-and-run-planning/03-02-SUMMARY.md`
</output>
