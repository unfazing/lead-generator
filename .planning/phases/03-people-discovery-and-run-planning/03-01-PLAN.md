---
phase: 03-people-discovery-and-run-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/people-search/schema.ts
  - src/lib/apollo/people-filter-definitions.ts
  - src/lib/apollo/people-search.ts
  - src/lib/db/schema/people-snapshots.ts
  - src/lib/db/repositories/people-snapshots.ts
  - src/app/api/people-search/route.ts
  - src/app/recipes/actions.ts
  - src/app/search/page.tsx
autonomous: true
requirements:
  - PEOP-01
  - PEOP-02
  - PEOP-03
  - PEOP-04
requirements_addressed:
  - PEOP-01
  - PEOP-02
  - PEOP-03
  - PEOP-04
must_haves:
  truths:
    - The user can run people search against either manually selected companies or all companies from the active company snapshot.
    - People search requests are validated against an Apollo-aligned server-side payload before execution.
    - People preview results are persisted as reusable local snapshots tied to the company selection context and source company snapshot.
  artifacts:
    - src/lib/apollo/people-search.ts
    - src/lib/db/repositories/people-snapshots.ts
    - src/app/api/people-search/route.ts
    - src/app/search/page.tsx
  key_links:
    - Selected company rows from the active company snapshot flow into the people search payload and snapshot metadata.
    - The people preview UI reads persisted people snapshot data rather than transient raw Apollo responses.
---

<objective>
Build the Phase 3 foundation for people discovery: company selection modes, Apollo-aligned people search execution, and persisted people preview snapshots.

Purpose: Turn company snapshot review into a reusable people preview workflow without introducing retrieval execution yet.
Output: Server-side people search path, people snapshot repository, and initial search-page integration for preview runs.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-people-discovery-and-run-planning/03-CONTEXT.md
@.planning/phases/02-company-search-and-snapshot-preview/02-VERIFICATION.md
@src/app/search/page.tsx
@src/lib/people-search/schema.ts
@src/lib/db/repositories/company-snapshots.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add people search execution and snapshot persistence</name>
  <files>src/lib/people-search/schema.ts, src/lib/apollo/people-search.ts, src/lib/db/schema/people-snapshots.ts, src/lib/db/repositories/people-snapshots.ts, src/app/api/people-search/route.ts</files>
  <read_first>src/lib/people-search/schema.ts, src/lib/apollo/company-search.ts, src/lib/db/repositories/company-snapshots.ts, .planning/phases/03-people-discovery-and-run-planning/03-CONTEXT.md</read_first>
  <action>Create a server-only people search module parallel to the company search stack. Validate the normalized people search payload against the supported Apollo contract, accept both “selected companies” and “all companies” modes, normalize Apollo people responses into preview-friendly rows, and persist them in a dedicated people snapshot repository with metadata linking back to the source company snapshot, source recipe pairing, and selection mode.</action>
  <verify>test -f src/lib/apollo/people-search.ts && test -f src/lib/db/repositories/people-snapshots.ts && test -f src/app/api/people-search/route.ts</verify>
  <acceptance_criteria>
    - server-side people search path exists and uses shared validation
    - people snapshot records preserve source company snapshot and selection-mode metadata
    - API path for people search stays server-owned and does not expose Apollo secrets
  </acceptance_criteria>
  <done>Phase 3 has a durable backend foundation for people preview runs.</done>
</task>

<task type="auto">
  <name>Task 2: Add company-selection modes and people preview workspace integration</name>
  <files>src/app/search/page.tsx, src/app/recipes/actions.ts, src/features/company-search/components/company-results-workspace.tsx, src/features/people-search/components/people-search-panel.tsx, src/features/people-search/components/people-results-table.tsx</files>
  <read_first>src/app/search/page.tsx, src/features/company-search/components/company-results-workspace.tsx, src/features/recipes/components/recipe-list.tsx, src/features/recipes/components/recipe-editor.tsx</read_first>
  <action>Extend the search page so operators can choose either all companies in the active company snapshot or a manual subset from the reviewed company results. Add a people search panel that uses the selected people recipe and the chosen company mode, then render the resulting people snapshot in a preview table with practical default columns. Keep the flow direct and on the search page; do not introduce a wizard.</action>
  <verify>test -f src/features/people-search/components/people-search-panel.tsx && test -f src/features/people-search/components/people-results-table.tsx</verify>
  <acceptance_criteria>
    - user can choose selected-companies mode or all-companies mode from the search page
    - people search can be launched from the current company+people recipe pairing
    - people preview renders from persisted snapshot data
    - company selection and people preview stay on `/search`
  </acceptance_criteria>
  <done>Operators can run and review people previews from company snapshot context.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] Manual smoke check confirms both company-selection modes can drive a people preview run without reaching retrieval execution
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The search page can now move from company snapshots to persisted people previews using explicit operator choices
</success_criteria>

<output>
After completion, create `.planning/phases/03-people-discovery-and-run-planning/03-01-SUMMARY.md`
</output>
