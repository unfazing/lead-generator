---
phase: 04-retrieval-execution-and-run-safety
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/lib/db/repositories/retrieval-runs.ts
  - src/lib/db/repositories/retrieval-run-items.ts
  - src/lib/retrieval/execution.ts
  - src/lib/retrieval/run-summary.ts
  - src/app/api/retrieval-runs/[runId]/route.ts
  - src/app/recipes/actions.ts
  - src/features/retrieval-runs/components/retrieval-run-status-card.tsx
  - src/features/retrieval-runs/components/retrieval-run-usage-summary.tsx
  - src/app/search/page.tsx
autonomous: true
requirements:
  - COST-04
  - EMAI-03
must_haves:
  truths:
    - The user can inspect an interrupted retrieval run and understand what was already processed versus what remains.
    - The user can resume a persisted interrupted run without silently restarting processed contacts from scratch.
    - After completion, the user can compare the saved plan estimate with actual run counts and credit usage.
  artifacts:
    - src/app/api/retrieval-runs/[runId]/route.ts
    - src/lib/retrieval/run-summary.ts
    - src/features/retrieval-runs/components/retrieval-run-usage-summary.tsx
    - src/lib/retrieval/execution.ts
  key_links:
    - Interrupted-state detection derives from persisted heartbeat and run status rather than client timers.
    - Resume logic selects unresolved run items from storage instead of replaying prior batch indexes.
    - Usage reconciliation compares immutable run-plan estimate fields with persisted actual batch counters on the retrieval run.
---

<objective>
Add resume-safe inspection and actual-versus-estimated usage reconciliation on top of the durable retrieval-run foundation.

Purpose: Make the operator trust interrupted and completed runs by showing exactly what happened and allowing safe continuation without double-processing.
Output: Interrupted-run detection, resume action, run summary API, and usage reconciliation UI.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-CONTEXT.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-RESEARCH.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-01-PLAN.md
@src/lib/retrieval/execution.ts
@src/lib/db/repositories/retrieval-runs.ts
@src/lib/db/repositories/retrieval-run-items.ts
@src/app/search/page.tsx

<interfaces>
Expected from 04-01:
```ts
type RetrievalRunStatus =
  | "ready"
  | "running"
  | "interrupted"
  | "completed"
  | "failed";

type RetrievalRunRecord = {
  id: string;
  runPlanId: string;
  peopleSnapshotId: string;
  status: RetrievalRunStatus;
  estimatedContacts: number;
  processedCount: number;
  lastCheckpointAt: string | null;
  lastHeartbeatAt: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement interruption detection, safe inspection, and resume targeting</name>
  <files>src/lib/db/repositories/retrieval-runs.ts, src/lib/db/repositories/retrieval-run-items.ts, src/lib/retrieval/execution.ts, src/app/recipes/actions.ts, src/lib/retrieval/run-summary.ts</files>
  <behavior>
    - Test 1: a stale heartbeat on a persisted `running` run is surfaced as `interrupted` during inspection.
    - Test 2: resume only re-queues unresolved run items and leaves completed or reused items untouched.
    - Test 3: the resume action exposes prior progress and remaining work before continuing, preventing silent restart.
  </behavior>
  <action>Layer EMAI-03 behavior on the 04-01 storage model per D-08, D-09, and D-10. Add visible-status derivation that marks stale `running` records as interrupted, plus read models that summarize processed, remaining, failed, and reusable items from persisted storage. Extend the execution service and server actions with an explicit resume path that rehydrates only unresolved items instead of restarting from batch position. Do not add optimistic client-only resume assumptions and do not rebuild pending work from transient UI state.</action>
  <verify>
    <automated>npm run test -- retrieval-resume run-summary && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Interrupted runs are inspectable from persisted state and can resume safely without double-processing completed contacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add actual-versus-estimated usage reconciliation and polling summary API</name>
  <files>src/lib/retrieval/run-summary.ts, src/app/api/retrieval-runs/[runId]/route.ts, src/features/retrieval-runs/components/retrieval-run-status-card.tsx, src/features/retrieval-runs/components/retrieval-run-usage-summary.tsx, src/app/search/page.tsx</files>
  <behavior>
    - Test 1: the run summary reports immutable estimate values from the run plan alongside actual processed, reused, missing, and credit-consumed counts.
    - Test 2: the polling route returns server-backed run status and reconciliation fields for active, interrupted, and completed runs.
    - Test 3: completed runs expose a practical estimate-vs-actual summary without recomputing from UI-only counts.
  </behavior>
  <action>Implement COST-04 and D-06 using persisted Apollo batch counters plus run-level rollups. Add a run-summary layer that joins the retrieval run, run items, and original run-plan estimate into one operator-facing summary. Expose that summary through a route handler for polling, then extend the workspace retrieval-run UI with a compact actual-versus-estimated usage section showing estimated contacts, actual attempted contacts, reused/deduped volume known so far, and credit consumption when Apollo returns it. Keep the estimate immutable and auditable; never overwrite plan fields with actual execution values.</action>
  <verify>
    <automated>npm run test -- retrieval-usage-summary && npm run lint && npm run typecheck</automated>
  </verify>
  <done>The operator can inspect interrupted or completed runs, poll for updated status, and compare original estimate with actual usage from persisted run data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] Resume/reconciliation automated tests cover stale-heartbeat interruption, unresolved-item resume, and usage rollup behavior
- [ ] An interrupted run is shown as interrupted from persisted heartbeat data after reload
- [ ] Resuming continues only unresolved contacts
- [ ] Completed runs display estimate-versus-actual usage in the workspace
</verification>

<success_criteria>
Interrupted runs are safely inspectable and resumable, and completed runs expose trustworthy actual-versus-estimated usage without mutating the original run plan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-retrieval-execution-and-run-safety/04-02-SUMMARY.md`
</output>
