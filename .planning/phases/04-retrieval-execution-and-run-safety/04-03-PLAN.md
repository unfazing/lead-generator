---
phase: 04-retrieval-execution-and-run-safety
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/lib/retrieval/preflight.ts
  - src/lib/retrieval/quality.ts
  - src/lib/db/repositories/retrieval-run-items.ts
  - src/lib/db/repositories/retrieval-runs.ts
  - src/lib/retrieval/execution.ts
  - src/lib/apollo/people-enrichment.ts
  - src/features/retrieval-runs/components/retrieval-run-status-card.tsx
  - src/features/retrieval-runs/components/retrieval-run-results-table.tsx
  - src/features/run-planning/components/retrieval-readiness-gate.tsx
  - src/app/search/page.tsx
autonomous: true
requirements:
  - COST-05
  - EMAI-04
must_haves:
  truths:
    - The user can avoid unnecessary repeat processing because contacts are classified for reuse or dedupe before Apollo enrichment starts.
    - The user can tell which retrieval outcomes are verified business emails versus non-verified or unusable results.
    - Reuse, dedupe, and quality outcomes remain stored with the retrieval run for later export and rerun phases.
  artifacts:
    - src/lib/retrieval/preflight.ts
    - src/lib/retrieval/quality.ts
    - src/features/retrieval-runs/components/retrieval-run-results-table.tsx
    - src/lib/db/repositories/retrieval-run-items.ts
  key_links:
    - Preflight classification writes run-item dispositions before any Apollo provider call is issued.
    - Apollo response mapping normalizes raw provider fields into internal quality categories on each run item.
    - The workspace surfaces reuse/dedupe and quality outcomes from persisted run items rather than recalculating them ad hoc.
---

<objective>
Finish Phase 4 by enforcing pre-enrichment dedupe/reuse checks and persisting normalized verified-email quality outcomes.

Purpose: Close the spend-protection and result-trust gap so Phase 5 export can build on stable retrieval outcomes without reinterpreting Apollo payloads.
Output: Preflight classifier, quality normalizer, persisted outcome categories, and operator-facing results detail.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-CONTEXT.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-RESEARCH.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-02-PLAN.md
@src/lib/retrieval/execution.ts
@src/lib/db/repositories/retrieval-run-items.ts
@src/app/search/page.tsx

<interfaces>
Expected from prior plans:
```ts
type RetrievalRunItemRecord = {
  id: string;
  runId: string;
  personApolloId: string;
  disposition: string;
  executionStatus: "pending" | "processing" | "completed" | "failed";
  outcomeQuality: string | null;
  reusedFromRunId: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preflight dedupe and reuse classification before enrichment calls</name>
  <files>src/lib/retrieval/preflight.ts, src/lib/db/repositories/retrieval-run-items.ts, src/lib/db/repositories/retrieval-runs.ts, src/lib/retrieval/execution.ts, src/features/run-planning/components/retrieval-readiness-gate.tsx</files>
  <behavior>
    - Test 1: duplicate contacts within the same run are marked `deduped_within_run` and never sent to Apollo.
    - Test 2: contacts with prior persisted verified or unusable terminal outcomes are marked as reused before execution begins.
    - Test 3: the run summary and kickoff UI expose pending-call versus reused/deduped counts before processing starts.
  </behavior>
  <action>Implement COST-05 per D-11 and D-13 by adding a server-side preflight classifier that turns reviewed people snapshot rows into run items with explicit dispositions such as `pending_call`, `reused_verified`, `reused_unusable`, and `deduped_within_run`. Wire this into kickoff and resume so reuse or dedupe decisions are persisted before Apollo is called. Surface the preflight summary near retrieval kickoff or run status so reused work stays visible in actual-versus-estimated reporting. Do not hide reused or deduped contacts as invisible skips.</action>
  <verify>
    <automated>npm run test -- retrieval-preflight && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Pre-enrichment dedupe and reuse decisions are persisted and visible before provider calls begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize Apollo outcomes into verified-email quality categories and expose run results detail</name>
  <files>src/lib/retrieval/quality.ts, src/lib/apollo/people-enrichment.ts, src/lib/retrieval/execution.ts, src/features/retrieval-runs/components/retrieval-run-status-card.tsx, src/features/retrieval-runs/components/retrieval-run-results-table.tsx, src/app/search/page.tsx</files>
  <behavior>
    - Test 1: verified Apollo business-email results map to a distinct `verified_business_email` internal quality.
    - Test 2: non-verified, unavailable, no-match, and provider-error responses map to separate unusable or non-verified internal categories.
    - Test 3: persisted run-item quality values drive the workspace results table and summary badges without relying on raw provider labels.
  </behavior>
  <action>Implement EMAI-04 per D-12 and D-13 with a central Apollo outcome normalizer in `src/lib/retrieval/quality.ts`. Map raw `match` and `bulk_match` responses into stable internal quality categories, persist those categories on run items, and keep the raw provider payload only as supporting detail. Add operator-facing results detail in the search-first workspace so verified business-email outcomes are visually preferred while non-verified, unavailable, no-match, and provider-error cases remain inspectable. Do not couple UI copy directly to Apollo field names, and do not expand into export formatting or waterfall enrichment.</action>
  <verify>
    <automated>npm run test -- retrieval-quality retrieval-results-table && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Retrieval runs persist stable verified-email quality categories and the workspace can distinguish export-ready verified outcomes from unusable ones.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] Dedupe and quality automated tests cover reuse classification and Apollo outcome normalization
- [ ] Preflight shows reused and deduped counts before Apollo calls
- [ ] Duplicate contacts are not reprocessed within a run
- [ ] Verified-business-email outcomes are clearly separated from non-verified or unusable results in the workspace
</verification>

<success_criteria>
Phase 4 ends with persisted preflight dedupe/reuse decisions, stable Apollo quality categories, and operator-visible run outcomes that Phase 5 export can trust.
</success_criteria>

<output>
After completion, create `.planning/phases/04-retrieval-execution-and-run-safety/04-03-SUMMARY.md`
</output>
