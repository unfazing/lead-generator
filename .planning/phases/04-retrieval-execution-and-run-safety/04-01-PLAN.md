---
phase: 04-retrieval-execution-and-run-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/db/client.ts
  - src/lib/db/repositories/file-storage.ts
  - src/lib/db/repositories/retrieval-runs.ts
  - src/lib/db/repositories/retrieval-run-items.ts
  - src/lib/apollo/people-enrichment.ts
  - src/lib/retrieval/execution.ts
  - src/app/recipes/actions.ts
  - src/features/run-planning/components/retrieval-readiness-gate.tsx
  - src/features/retrieval-runs/components/retrieval-run-status-card.tsx
  - src/app/search/page.tsx
autonomous: true
requirements:
  - EMAI-02
must_haves:
  truths:
    - The user can start a verified-email retrieval run only from a reviewed people snapshot with a confirmed run plan.
    - The user can see persisted run status and processed counts from the frontend while a run is active.
    - Reloading the app still shows the latest known retrieval-run state instead of falling back to client memory.
    - Only one retrieval run can hold the active execution lease at a time, so kickoff/resume never races another tab into double-processing.
    - The executor applies explicit batch throttling, retry backoff, and Apollo rate-limit handling while persisting progress.
  artifacts:
    - package.json
    - src/lib/db/repositories/retrieval-runs.ts
    - src/lib/db/repositories/retrieval-run-items.ts
    - src/lib/apollo/people-enrichment.ts
    - src/lib/retrieval/execution.ts
    - src/features/retrieval-runs/components/retrieval-run-status-card.tsx
  key_links:
    - Confirmed run plans create retrieval-run records before any Apollo enrichment call is attempted.
    - Kickoff and resume both require a persisted active-run lease before the executor can mark items `processing`.
    - The execution loop persists batch-boundary checkpoints and run counters after each Apollo call.
    - Execution pacing, retry backoff, and rate-limit responses are handled inside the server executor rather than ad hoc in the UI.
    - The search-first workspace renders server-backed run status from retrieval-run repositories rather than client-local state.
---

<objective>
Create the durable retrieval-run foundation for Phase 4: explicit Apollo enrichment kickoff, persisted execution exclusivity, throttled bulk-first processing, and live server-backed progress visibility.

Purpose: Land the core execution path first so later resume, reconciliation, and dedupe work builds on durable state instead of extending the old run-plan record.
Output: Retrieval-run repositories, Apollo enrichment service, lease-aware batch executor, kickoff action, test harness bootstrap, and workspace status card.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-CONTEXT.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-RESEARCH.md
@.planning/phases/03.1-rework-the-search-flow/03.1-rework-the-search-flow-03-SUMMARY.md
@.planning/phases/04-retrieval-execution-and-run-safety/04-VALIDATION.md
@src/app/recipes/actions.ts
@src/lib/db/repositories/run-plans.ts
@src/features/run-planning/components/retrieval-readiness-gate.tsx
@src/app/search/page.tsx

<interfaces>
From src/lib/db/repositories/run-plans.ts:
```ts
export type RunPlanRecord = {
  id: string;
  companyRecipeId: string;
  peopleRecipeId: string;
  companySnapshotId: string;
  peopleSnapshotId: string;
  maxContacts: number;
  estimatedContacts: number;
  estimateSummary: string;
  estimateNote: string;
  status: RunPlanStatus;
  confirmedAt: string | null;
  createdAt: string;
  updatedAt: string;
};
```

From src/features/run-planning/components/retrieval-readiness-gate.tsx:
```ts
export function RetrievalReadinessGate({ plan }: { plan: RunPlanRecord })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish Wave 0 test infrastructure for retrieval-run durability and executor behavior</name>
  <files>package.json, tests/retrieval-runs.repository.test.ts, tests/people-enrichment.execution.test.ts, tests/retrieval-resume.test.ts, tests/retrieval-preflight.test.ts, tests/retrieval-quality.test.ts</files>
  <behavior>
    - Test 1: the repo gains a real `npm run test` command suitable for retrieval-domain unit and integration tests.
    - Test 2: retrieval-domain tests can be targeted with focused commands instead of relying only on lint/typecheck.
    - Test 3: the validation strategy's named Wave 0 test files exist as concrete assets before risky execution code lands.
    - Test 4: the first retrieval-domain smoke run proves the new runner, scripts, and Wave 0 test files execute successfully before behavior-specific implementation begins.
  </behavior>
  <action>Implement the validation contract in `04-VALIDATION.md` before the execution layer grows. Add the Phase 4 Wave 0 test harness to the repo, including a real test runner entry in `package.json` plus the explicit test files named in the validation strategy: `tests/retrieval-runs.repository.test.ts`, `tests/people-enrichment.execution.test.ts`, `tests/retrieval-resume.test.ts`, `tests/retrieval-preflight.test.ts`, and `tests/retrieval-quality.test.ts`. Seed them with the minimum fixtures and smoke coverage required for repository durability, executor batching, throttling, resume behavior, preflight classification, and Apollo outcome normalization. Do not leave Phase 4 marked `tdd=\"true\"` while verifying only with lint and typecheck.</action>
  <verify>
    <automated>npm run test -- retrieval-runs.repository people-enrichment.execution retrieval-resume retrieval-preflight retrieval-quality && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Phase 4 has a concrete automated test harness and can verify retrieval behavior beyond static checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create durable retrieval-run storage with active-run lease safety</name>
  <files>src/lib/db/client.ts, src/lib/db/repositories/file-storage.ts, src/lib/db/repositories/retrieval-runs.ts, src/lib/db/repositories/retrieval-run-items.ts</files>
  <behavior>
    - Test 1: creating a retrieval run from a ready run plan writes a run header plus per-contact item rows without mutating the original plan estimate.
    - Test 2: updating a batch checkpoint persists processed counts, heartbeat, and item terminal states across a reload.
    - Test 3: kickoff or resume cannot acquire a second active execution lease while another run already holds it.
    - Test 4: malformed or missing JSON storage is rejected or recovered through schema-validated repository reads instead of blind casting.
  </behavior>
  <action>Introduce a new durable retrieval-run model per D-03, D-08, D-10, and the planner's locked decision to not rely on `run-plans.json` alone. Add shared atomic file helpers in `src/lib/db/repositories/file-storage.ts` and fix `src/lib/db/client.ts` so every repository can ensure its own target directory instead of only the recipe path. Implement separate retrieval-run header and retrieval-run item repositories with Zod-backed read/write validation, immutable linkage to `runPlanId` / `peopleSnapshotId`, batch checkpoint fields, heartbeat fields, and counters needed for EMAI-02. Include persisted active-run lease or lock metadata plus conflict handling so kickoff/resume can guarantee single-run exclusivity in the file-backed store. Do not fold this data back into the existing run-plan record beyond foreign-key references.</action>
  <verify>
    <automated>npm run test -- retrieval-runs && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Durable retrieval-run repositories exist, persist atomically, and enforce single-active-run safety for Phase 4 execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Apollo bulk-first kickoff, throttling, and persisted progress updates</name>
  <files>src/lib/apollo/people-enrichment.ts, src/lib/retrieval/execution.ts, src/app/recipes/actions.ts, src/features/run-planning/components/retrieval-readiness-gate.tsx</files>
  <behavior>
    - Test 1: kickoff from a confirmed plan creates a retrieval run, schedules items, and uses `bulk_match` for batches up to 10 contacts.
    - Test 2: a single-item remainder or targeted retry uses `match` rather than forcing a one-item bulk request.
    - Test 3: each completed batch updates run/item persistence before the next batch starts.
    - Test 4: executor throttling, retry backoff, and Apollo rate-limit responses are persisted and respected between batches.
  </behavior>
  <action>Implement the explicit Apollo enrichment path around `people/bulk_match` and `people/match` per D-04 and the locked user decision: bulk-first by default, single `match` only for single-item remainder or retry. Add a server-owned Apollo enrichment service module and a retrieval executor that reads pending run items, groups them into batches, writes `processing` state, persists terminal outcomes at batch boundaries, and updates heartbeat plus processed counters after each call. Plan explicit pacing, retry backoff, and Apollo rate-limit handling as part of the executor contract, with persisted state that explains whether the run is active, cooling down, or retrying after provider pressure. Extend the readiness gate and `src/app/recipes/actions.ts` with an explicit kickoff action that only starts from a confirmed run plan and returns quickly after persisting the run start; do not turn the whole run into one long server action.</action>
  <verify>
    <automated>npm run test -- people-enrichment execution && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Confirmed plans can start a durable Apollo enrichment run through a bulk-first, throttled, lease-aware server executor with persisted checkpoints.</done>
</task>

<task type="auto">
  <name>Task 4: Surface active retrieval-run status in the search-first workspace</name>
  <files>src/features/retrieval-runs/components/retrieval-run-status-card.tsx, src/app/search/page.tsx, src/features/run-planning/components/retrieval-readiness-gate.tsx</files>
  <action>Preserve the current search-first workflow and inline operator UX per D-05, D-07 and the locked decision to keep monitoring close to run planning. Add a compact retrieval-run status card on `/search` that reads persisted run state server-side and shows visible status, processed counts, batch/checkpoint timing, and whether the latest run is active or waiting. Keep this tied to the current people snapshot / run-plan context rather than inventing a separate admin surface or wizard.</action>
  <verify>
    <automated>npm run test -- retrieval-run-status-card && npm run lint && npm run typecheck</automated>
  </verify>
  <done>The workspace shows live server-backed retrieval progress and status alongside the existing run-planning flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] Retrieval-domain automated tests run through the new Phase 4 test harness
- [ ] Starting retrieval from a confirmed plan creates a persisted retrieval run and displays it on `/search`
- [ ] Kickoff or resume cannot acquire a second active execution lease while another run already holds it
- [ ] Multi-contact runs default to `bulk_match`, with `match` used only for single-item remainder or retry
- [ ] Executor throttling and retry backoff are visible in persisted run status rather than hidden in transient process state
</verification>

<success_criteria>
The codebase has a new durable retrieval-run layer, confirmed plans can kick off a server-owned enrichment run, and the search workspace shows persisted progress while the run is active.
</success_criteria>

<output>
After completion, create `.planning/phases/04-retrieval-execution-and-run-safety/04-01-SUMMARY.md`
</output>
