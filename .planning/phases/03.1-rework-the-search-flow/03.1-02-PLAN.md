---
phase: 03.1-rework-the-search-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 03.1-01
files_modified:
  - src/app/search/company/[snapshotId]/page.tsx
  - src/app/search/people/page.tsx
  - src/features/company-search/components/company-results-workspace.tsx
  - src/features/people-search/components/people-search-panel.tsx
  - src/lib/people-search/schema.ts
  - src/lib/recipes/schema.ts
  - src/features/recipes/lib/recipe-form.ts
  - src/lib/db/repositories/recipes.ts
autonomous: true
requirements:
  - COMP-02
  - COMP-03
  - COMP-04
  - PEOP-01
  - PEOP-02
  - PEOP-04
must_haves:
  truths:
    - "User reviews a persisted company snapshot as a first-class record in the company workflow."
    - "People search starts from a people recipe plus one or more company snapshot sources instead of a downstream company-review page."
    - "Selected companies or all companies from chosen company snapshots become recipe-level organization IDs with persisted provenance."
    - "People search does not auto-run when organization IDs are imported into a recipe."
  artifacts:
    - path: "src/app/search/company/[snapshotId]/page.tsx"
      provides: "Company snapshot review stage as a first-class company-workflow record"
    - path: "src/app/search/people/page.tsx"
      provides: "People-search workflow page with recipe selection and company-snapshot import controls"
    - path: "src/features/people-search/components/people-search-panel.tsx"
      provides: "People-search form that applies selected-company or all-company snapshot imports to recipes explicitly before search execution"
    - path: "src/lib/recipes/schema.ts"
      provides: "People-recipe provenance fields for imported organization IDs"
  key_links:
    - from: "src/app/search/company/[snapshotId]/page.tsx"
      to: "src/features/company-search/components/company-results-workspace.tsx"
      via: "first-class company snapshot review with explicit params and selection"
      pattern: "CompanyResultsWorkspace"
    - from: "src/features/people-search/components/people-search-panel.tsx"
      to: "src/lib/db/repositories/recipes.ts"
      via: "explicit apply-companies action that updates people recipe organization IDs and provenance"
      pattern: "updatePeopleRecipe"
---

<objective>
Make company snapshots first-class records in the company workflow and let the people workflow import organization IDs from one or more company snapshots into people recipes with persisted provenance.

Purpose: Match the clarified product model where company search creates snapshots, and people search uses those snapshots to augment people recipes before an explicit people search run.
Output: Company snapshot review stage, people workflow page with snapshot-source import controls, and people-recipe schema/repository updates for organization-ID provenance.
</objective>

<execution_context>
@/Users/lian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
@.planning/phases/02-company-search-and-snapshot-preview/02-03-SUMMARY.md
@.planning/phases/03-people-discovery-and-run-planning/03-01-SUMMARY.md
@src/features/company-search/components/company-results-workspace.tsx
@src/features/people-search/components/people-search-panel.tsx
@src/lib/recipes/schema.ts
@src/lib/db/repositories/recipes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep company snapshots first-class and expose them as import sources for people workflow</name>
  <files>src/app/search/company/[snapshotId]/page.tsx, src/app/search/people/page.tsx, src/features/company-search/components/company-results-workspace.tsx, src/features/people-search/components/people-search-panel.tsx</files>
  <read_first>
    - .planning/phases/03.1-rework-the-search-flow/03.1-01-PLAN.md
    - src/features/company-search/components/company-results-workspace.tsx
    - src/features/people-search/components/people-search-panel.tsx
    - .planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
  </read_first>
  <action>Move company snapshot review into `/search/company/[snapshotId]` as a first-class company workflow record. The review stage must show broad-search warnings, exact snapshot params, row selection, and compact summary UI, but it must not render people results or run-planning controls. Rewrite `/search/people` so it starts from a people recipe plus one or more company snapshots chosen as organization-ID sources. On this page, the user must be able to review company snapshots, choose either selected companies or all qualified companies from each chosen snapshot, and prepare an explicit recipe update action instead of treating people search as downstream from one active company-review page.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Company review is isolated to `/search/company/[snapshotId]`, `/search/people` treats company snapshots as import sources rather than a downstream prerequisite page, and each chosen snapshot supports either selected-company or all-company import mode explicitly.</acceptance_criteria>
  <done>Company snapshots are first-class in the company workflow and the people workflow can use them as explicit organization-ID sources.</done>
</task>

<task type="auto">
  <name>Task 2: Persist organization IDs and provenance directly on people recipes before search execution</name>
  <files>src/lib/people-search/schema.ts, src/lib/recipes/schema.ts, src/features/recipes/lib/recipe-form.ts, src/lib/db/repositories/recipes.ts, src/features/people-search/components/people-search-panel.tsx</files>
  <read_first>
    - src/lib/people-search/schema.ts
    - src/lib/recipes/schema.ts
    - src/lib/db/repositories/recipes.ts
    - .planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
  </read_first>
  <action>Add explicit people-recipe fields for imported organization IDs provenance: snapshot IDs, import mode (`selected` or `all`) per snapshot, selected company IDs when relevant, and timestamp metadata. Implement an explicit `Apply companies to recipe` action on `/search/people` that updates the saved people recipe directly with the union of organization IDs from the chosen company snapshots, whether sourced from selected companies or all qualified companies, plus provenance of all contributing snapshot sources and modes. Do not auto-run people search after apply; the updated people recipe should be visible as the current source of truth for a later explicit search action.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Applying selected-company or all-company imports from one or more company snapshots updates the saved people recipe directly with organization IDs, import-mode-aware provenance, and people search remains an explicit follow-up action rather than an automatic side effect.</acceptance_criteria>
  <done>People recipes now store imported organization IDs plus per-snapshot provenance and import mode from company snapshots, and search execution remains an explicit next step.</done>
</task>

</tasks>

<verification>
Run `npm run lint && npm run typecheck`, then walk the intended flow contract: company workflow produces snapshots, people workflow imports company-derived organization IDs into a people recipe, and no automatic people search occurs on import.
</verification>

<success_criteria>
Company snapshots are first-class records, and people recipes can import organization IDs plus provenance from one or more company snapshots before an explicit people search run.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-rework-the-search-flow/03.1-rework-the-search-flow-02-SUMMARY.md`
</output>
