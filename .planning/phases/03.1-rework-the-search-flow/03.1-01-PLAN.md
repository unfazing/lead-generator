---
phase: 03.1-rework-the-search-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/search/page.tsx
  - src/app/search/company/page.tsx
  - src/app/search/people/page.tsx
  - src/features/search-workspace/lib/workspace-route-state.ts
  - src/features/search-workspace/components/workspace-empty-state.tsx
  - src/features/search-workspace/components/workspace-stage-nav.tsx
autonomous: true
requirements:
  - RECP-04
must_haves:
  truths:
    - "User lands on an instructions-first search entry instead of an implicitly selected workflow state."
    - "Company search and people search live at separate routes with separate entry states."
    - "Refreshing either entry route preserves only explicit context and never falls back to the first saved recipe or snapshot."
    - "User can load an existing saved recipe and rerun the relevant search workflow without re-entering filters."
  artifacts:
    - path: "src/features/search-workspace/lib/workspace-route-state.ts"
      provides: "Typed parser for explicit company-search and people-search route context"
      exports: ["parseSearchWorkspaceContext", "buildSearchWorkspaceQuery"]
    - path: "src/app/search/page.tsx"
      provides: "Instructions-first search landing page with explicit workflow choices"
    - path: "src/app/search/company/page.tsx"
      provides: "Explicit company-search workflow entry page"
    - path: "src/app/search/people/page.tsx"
      provides: "Explicit people-search workflow entry page"
    - path: "src/features/search-workspace/components/workspace-stage-nav.tsx"
      provides: "Compact workflow navigation shell"
  key_links:
    - from: "src/app/search/page.tsx"
      to: "src/features/search-workspace/lib/workspace-route-state.ts"
      via: "route/query parsing for explicit workflow context"
      pattern: "parseSearchWorkspaceContext"
    - from: "src/app/search/page.tsx"
      to: "src/app/search/company/page.tsx"
      via: "explicit workflow choice"
      pattern: "/search/company"
    - from: "src/app/search/page.tsx"
      to: "src/app/search/people/page.tsx"
      via: "explicit workflow choice"
      pattern: "/search/people"
---

<objective>
Make the search workspace instructions-first and split it into separate company-search and people-search entry routes with explicit route context.

Purpose: Establish the decoupled search-workflow contract so company search creates snapshots and people search starts from people recipes plus company-snapshot sources instead of one linear stacked page.
Output: Search workspace route parser, instructions landing page, explicit company and people entry pages, and reusable workflow navigation/empty-state components.
</objective>

<execution_context>
@/Users/lian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
@src/app/search/page.tsx
@src/features/recipes/components/recipe-list.tsx
@src/features/company-search/components/company-search-panel.tsx
@src/features/people-search/components/people-search-panel.tsx

<interfaces>
From src/app/search/page.tsx:
```typescript
type SearchPageProps = {
  searchParams?: Promise<Record<string, string | string[] | undefined>>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define explicit company-search and people-search route contracts</name>
  <files>src/features/search-workspace/lib/workspace-route-state.ts</files>
  <read_first>
    - .planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
    - src/app/search/page.tsx
  </read_first>
  <action>Create a typed route-state helper that parses and validates explicit workflow context for `/search`, `/search/company`, and `/search/people`: optional `companyRecipe`, optional `peopleRecipe`, optional source snapshot references for people workflow, and no implicit first-record fallback. Add helper functions to rebuild query strings for workflow links and redirects. Keep Apollo-facing payload state out of this helper; it should only model route context, selected workflow, and guard invalid combinations.</action>
  <verify>
    <automated>npm run lint -- src/features/search-workspace/lib/workspace-route-state.ts</automated>
  </verify>
  <acceptance_criteria>The route helper exports reusable parsing/building functions that reject invalid query combinations, support separate company/people workflows, and never synthesize a default recipe or snapshot ID from stored data.</acceptance_criteria>
  <done>Executors have a single source of truth for explicit company-search and people-search route context before the workflow pages are rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the stacked search landing page with instructions-first company and people workflow entry pages</name>
  <files>src/app/search/page.tsx, src/app/search/company/page.tsx, src/app/search/people/page.tsx, src/features/search-workspace/components/workspace-empty-state.tsx, src/features/search-workspace/components/workspace-stage-nav.tsx</files>
  <read_first>
    - src/app/search/page.tsx
    - src/features/recipes/components/recipe-list.tsx
    - src/features/company-search/components/company-search-panel.tsx
    - src/features/people-search/components/people-search-panel.tsx
    - .planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
  </read_first>
  <action>Rewrite `/search` into an instructions-first landing page that lets the user choose between company search and people search. Add `/search/company` as the explicit company-search workflow entry and `/search/people` as the explicit people-search workflow entry. The company route must load an existing company recipe and rerun company search without re-entering filters on the recipe page. The people route must load an existing people recipe and show snapshot-source selection controls without auto-running people search. Use shared empty-state and workflow-nav components for missing context instead of loading the first available recipe or snapshot. Keep the UI less wordy than the current page and do not reintroduce stacked downstream stages.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>`/search` no longer auto-selects recipes or snapshots, `/search/company` and `/search/people` act as separate workflow entries, and an existing saved recipe can be loaded on the relevant route to rerun that workflow without re-entering filters.</acceptance_criteria>
  <done>The search workspace now starts from explicit route state, supports existing-recipe rerun entry, and separates company-search from people-search entry behavior.</done>
</task>

</tasks>

<verification>
Run `npm run lint && npm run typecheck`, then confirm the route contract is sufficient to navigate from `/search` to `/search/company` and `/search/people` without depending on implicit first-item selection.
</verification>

<success_criteria>
The search workspace has an instructions-first entry, separate company and people workflow entry routes, and no first-recipe or first-snapshot fallback in its routing contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-rework-the-search-flow/03.1-rework-the-search-flow-01-SUMMARY.md`
</output>
