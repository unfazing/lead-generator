---
phase: 03.1-rework-the-search-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/search/page.tsx
  - src/app/search/company/page.tsx
  - src/features/search-workspace/lib/workspace-route-state.ts
  - src/features/search-workspace/components/workspace-empty-state.tsx
  - src/features/search-workspace/components/workspace-stage-nav.tsx
autonomous: true
requirements:
  - RECP-04
must_haves:
  truths:
    - "User lands on an instructions-first search entry instead of an implicitly selected workflow state."
    - "User can enter the company-search stage only through explicit recipe context in the route."
    - "Refreshing the entry or company-search stage preserves the intended workflow context without falling back to the first saved recipe."
  artifacts:
    - path: "src/features/search-workspace/lib/workspace-route-state.ts"
      provides: "Typed parser for stage route params and recipe pairing context"
      exports: ["parseSearchWorkspaceContext", "buildSearchWorkspaceQuery"]
    - path: "src/app/search/page.tsx"
      provides: "Instructions-first search entry page"
    - path: "src/app/search/company/page.tsx"
      provides: "Explicit company-search stage page"
    - path: "src/features/search-workspace/components/workspace-stage-nav.tsx"
      provides: "Compact stage summary and navigation shell"
  key_links:
    - from: "src/app/search/page.tsx"
      to: "src/features/search-workspace/lib/workspace-route-state.ts"
      via: "route/query parsing for explicit recipe context"
      pattern: "parseSearchWorkspaceContext"
    - from: "src/app/search/company/page.tsx"
      to: "src/features/search-workspace/components/workspace-stage-nav.tsx"
      via: "shared stage navigation"
      pattern: "WorkspaceStageNav"
---

<objective>
Make the search workspace instructions-first and route-driven so workflow state starts from explicit recipe context instead of implicit first-item fallbacks.

Purpose: Establish the stage contract recommended by research before moving company, people, and plan review into separate pages.
Output: Search workspace route parser, instructions entry page, company stage page, and reusable stage navigation/empty-state components.
</objective>

<execution_context>
@/Users/lian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
@src/app/search/page.tsx
@src/features/recipes/components/recipe-list.tsx
@src/features/company-search/components/company-search-panel.tsx

<interfaces>
From src/app/search/page.tsx:
```typescript
type SearchPageProps = {
  searchParams?: Promise<Record<string, string | string[] | undefined>>;
};
```

From src/features/company-search/components/company-search-panel.tsx:
```typescript
type CompanySearchPanelProps = {
  recipe: CompanyRecipe | null;
  pairedPeopleRecipe: PeopleRecipe | null;
  snapshot: CompanySnapshotRecord | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define explicit search-workspace route contracts</name>
  <files>src/features/search-workspace/lib/workspace-route-state.ts</files>
  <read_first>
    - .planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
    - src/app/search/page.tsx
  </read_first>
  <action>Create a typed route-state helper that parses and validates the explicit search-workspace context recommended in research: optional `companyRecipe` and `peopleRecipe` pairing for entry/company stages, no implicit first-record fallback, and helper functions to rebuild query strings for stage links and redirects. Keep Apollo-facing state out of this helper; this file should only model route context and guard invalid combinations per D-01 route-driven workflow.</action>
  <verify>
    <automated>npm run lint -- src/features/search-workspace/lib/workspace-route-state.ts</automated>
  </verify>
  <acceptance_criteria>The route helper exports reusable parsing/building functions that reject invalid query combinations and never synthesize a default recipe ID from stored data.</acceptance_criteria>
  <done>Executors have a single source of truth for search workspace route context before any stage pages are rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the stacked search landing page with instructions-first entry and explicit company stage</name>
  <files>src/app/search/page.tsx, src/app/search/company/page.tsx, src/features/search-workspace/components/workspace-empty-state.tsx, src/features/search-workspace/components/workspace-stage-nav.tsx</files>
  <read_first>
    - src/app/search/page.tsx
    - src/features/recipes/components/recipe-list.tsx
    - src/features/company-search/components/company-search-panel.tsx
    - .planning/phases/03.1-rework-the-search-flow/03.1-RESEARCH.md
  </read_first>
  <action>Rewrite `/search` into an instructions/pairing entry page per the locked decision that recipe pages default to instructions, not implicit selections. Add `/search/company` as the first active stage page that requires explicit company-recipe context, shows compact stage navigation, and renders the existing company search panel only when route pairing is valid. Use a shared empty-state component for missing pairing instead of loading the first available recipe. Keep the UI less wordy than the current page and do not reintroduce stacked downstream stages.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>`/search` no longer auto-selects recipes or snapshots, and `/search/company` renders only the company-search stage with explicit context and compact stage navigation.</acceptance_criteria>
  <done>The search workspace now starts from explicit route state and the company-search stage is isolated from later review/planning stages.</done>
</task>

</tasks>

<verification>
Run `npm run lint && npm run typecheck`, then confirm the route contract is sufficient to navigate from `/search` to `/search/company` without depending on implicit first-item selection.
</verification>

<success_criteria>
The search workspace has an instructions-first entry, explicit company stage, and no first-recipe or first-snapshot fallback in its routing contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-rework-the-search-flow/03.1-rework-the-search-flow-01-SUMMARY.md`
</output>
