---
phase: 04.1-contact-batch-enrichment-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/repositories/contact-batches.ts
  - src/lib/db/repositories/contact-batch-members.ts
  - src/lib/db/repositories/enriched-people.ts
  - src/app/enrich/page.tsx
  - src/app/enrich/store/page.tsx
  - src/lib/retrieval/execution.ts
autonomous: true
requirements:
  - COST-05
  - EMAI-04
must_haves:
  truths:
    - "Contact batches are independent from recipes and persist mutable member groupings keyed by Apollo person ID."
    - "Batch members preserve lightweight frozen display fields plus all provenance references from contributing people snapshots."
    - "The global enriched-people store remains append-only and has its own dedicated viewer route."
    - "Retrieval execution enforces global no-reenrichment by Apollo person ID regardless of batch."
  artifacts:
    - path: "src/lib/db/repositories/contact-batches.ts"
      provides: "Contact batch persistence model"
    - path: "src/lib/db/repositories/contact-batch-members.ts"
      provides: "Batch member persistence with provenance and dedupe semantics"
    - path: "src/app/enrich/store/page.tsx"
      provides: "Dedicated central enriched-people store viewer"
  key_links:
    - from: "src/lib/db/repositories/contact-batch-members.ts"
      to: "src/lib/db/repositories/enriched-people.ts"
      via: "batch member status can reflect already-enriched people from global store"
      pattern: "personApolloId"
    - from: "src/lib/retrieval/execution.ts"
      to: "src/lib/db/repositories/enriched-people.ts"
      via: "global skip-before-Apollo enforcement"
      pattern: "getEnrichedPeopleByApolloIds"
---

<objective>
Create the persistence and route foundations for independent contact batches and a separate global enriched-people store viewer.

Purpose: Establish the durable data model that lets enrichment move out of the people workflow while preserving the append-only global cache.
Output: Contact batch repositories, batch-member provenance model, `/enrich` and `/enrich/store` route foundations, and enforced no-reenrichment semantics.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
@src/lib/db/repositories/enriched-people.ts
@src/lib/retrieval/execution.ts
@src/app/search/people/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contact batch and contact batch member persistence with provenance-aware dedupe</name>
  <files>src/lib/db/repositories/contact-batches.ts, src/lib/db/repositories/contact-batch-members.ts</files>
  <read_first>
    - .planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
    - src/lib/db/repositories/enriched-people.ts
  </read_first>
  <action>Create repositories and schemas for contact batches plus contact-batch members. Members must dedupe by Apollo person ID, retain frozen display fields (`fullName`, `title`, `companyName`, `companyApolloId`), and merge all provenance references from contributing people snapshots. Batch membership must support later removal without affecting the global enriched-people store.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Contact batches can be created, edited, deleted, and populated with unique Apollo person IDs while preserving all source-snapshot provenance refs for each member.</acceptance_criteria>
  <done>The app has a durable grouping model above people snapshots and the global enriched-person cache.</done>
</task>

<task type="auto">
  <name>Task 2: Add `/enrich` and `/enrich/store` route foundations with global no-reenrichment enforcement</name>
  <files>src/app/enrich/page.tsx, src/app/enrich/store/page.tsx, src/lib/retrieval/execution.ts, src/lib/db/repositories/enriched-people.ts</files>
  <read_first>
    - src/lib/retrieval/execution.ts
    - src/lib/db/repositories/enriched-people.ts
    - .planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
  </read_first>
  <action>Add route foundations for `/enrich` and `/enrich/store`. The store route must render the append-only enriched-people dataset as a first-class viewer. Ensure retrieval execution still checks the global enriched-people store by Apollo person ID before any Apollo call, so cross-batch duplication remains impossible even as the UI model changes.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>The app has explicit enrichment routes and the executor still guarantees that any Apollo person ID already in the central store is skipped before Apollo enrichment.</acceptance_criteria>
  <done>The enrichment workflow has dedicated routes and a stable global reuse foundation.</done>
</task>

</tasks>

<verification>
Run `npm run lint && npm run typecheck && npm run build`, then confirm the app has dedicated `/enrich` and `/enrich/store` routes and that the retrieval executor still uses the global enriched-people store for Apollo-call skipping.
</verification>

<success_criteria>
Contact batches and the global enriched-people store are first-class persisted concepts, with dedicated enrichment routes and backend-enforced cross-batch no-reenrichment.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-contact-batch-enrichment-workflow/04.1-contact-batch-enrichment-workflow-01-SUMMARY.md`
</output>
