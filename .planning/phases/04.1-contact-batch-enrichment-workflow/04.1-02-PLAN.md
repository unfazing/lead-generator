---
phase: 04.1-contact-batch-enrichment-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - src/app/enrich/page.tsx
  - src/features/enrich/components/contact-batch-panel.tsx
  - src/features/enrich/components/contact-batch-list.tsx
  - src/features/enrich/components/add-to-batch-from-snapshot.tsx
  - src/app/recipes/actions.ts
  - src/app/search/people/page.tsx
autonomous: true
requirements:
  - PEOP-04
  - EMAI-01
must_haves:
  truths:
    - "The primary enrichment workflow is keyed by contact batches in the left rail, not by recipes."
    - "Users can add people into a batch from saved people snapshots inside the enrich workflow page."
    - "Users can create a new batch or add to an existing batch from within the enrich workflow."
    - "Batch member rows show enrichment state based on the global enriched-people store."
  artifacts:
    - path: "src/app/enrich/page.tsx"
      provides: "Main contact batch enrichment workflow route"
    - path: "src/features/enrich/components/contact-batch-panel.tsx"
      provides: "Batch viewer and actions shell"
    - path: "src/features/enrich/components/add-to-batch-from-snapshot.tsx"
      provides: "People-snapshot-based add-to-batch tool"
  key_links:
    - from: "src/features/enrich/components/add-to-batch-from-snapshot.tsx"
      to: "src/lib/db/repositories/contact-batch-members.ts"
      via: "add selected snapshot people into an existing or new batch"
      pattern: "personApolloId"
    - from: "src/app/search/people/page.tsx"
      to: "src/app/enrich/page.tsx"
      via: "handoff into the dedicated enrichment workflow"
      pattern: "/enrich"
---

<objective>
Build the main `/enrich` workflow around contact batches and batch-member management from saved people snapshots.

Purpose: Move the operator-facing enrichment workflow out of the people search page and into a reusable batch-centric interface.
Output: Contact-batch left rail, batch viewer, add-from-snapshot flow, and clear batch-state presentation.
</objective>

<context>
@.planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
@src/app/search/people/page.tsx
@src/features/people-search/components/saved-people-snapshots-panel.tsx
@src/features/recipes/components/recipe-list.tsx
@src/features/snapshots/components/data-snapshot-viewer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the batch-centric enrich workspace and left rail</name>
  <files>src/app/enrich/page.tsx, src/features/enrich/components/contact-batch-list.tsx, src/features/enrich/components/contact-batch-panel.tsx</files>
  <read_first>
    - src/features/recipes/components/recipe-list.tsx
    - .planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
  </read_first>
  <action>Create the `/enrich` page with a left rail for contact batches and a right-pane batch viewer. The left rail must support new/edit/delete. The batch viewer should show default columns like name, title, company, source count, enrichment state, email, and email status, with provenance details kept compact behind disclosures or secondary detail views.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>`/enrich` is a first-class route centered on contact batches, with a left rail for batch management and a right pane for batch members plus batch-level actions.</acceptance_criteria>
  <done>The enrichment workspace is centered on contact batches instead of recipes or people snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Add people into contact batches from saved people snapshots inside the enrich workflow</name>
  <files>src/features/enrich/components/add-to-batch-from-snapshot.tsx, src/app/enrich/page.tsx, src/app/recipes/actions.ts, src/app/search/people/page.tsx</files>
  <read_first>
    - src/features/people-search/components/saved-people-snapshots-panel.tsx
    - src/app/search/people/page.tsx
    - .planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
  </read_first>
  <action>Within `/enrich`, let the user choose saved people snapshots, select people from those snapshots, and add them into the current batch or a newly created batch. Do not allow manual Apollo ID entry. Preserve all provenance refs per member while deduping the actual member row by Apollo person ID. Also add a clear handoff/action from the people workflow into `/enrich` instead of embedding enrichment as the terminal state there.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>People can be selected from saved people snapshots and added into a current or new contact batch entirely within `/enrich`, and the people workflow no longer acts as the primary enrichment home.</acceptance_criteria>
  <done>Contact batches can be populated from multiple saved people snapshots inside the dedicated enrichment workflow.</done>
</task>

</tasks>

<verification>
Run `npm run lint && npm run typecheck && npm run build`, then confirm the operator can create/select a contact batch and add people into it from saved people snapshots without using the people workflow as the main enrichment page.
</verification>

<success_criteria>
The main enrichment workflow is now batch-centric, and people snapshots are just source material for adding members into those batches.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-contact-batch-enrichment-workflow/04.1-contact-batch-enrichment-workflow-02-SUMMARY.md`
</output>
