---
phase: 04.1-contact-batch-enrichment-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 04.1-02
files_modified:
  - src/app/recipes/actions.ts
  - src/lib/retrieval/execution.ts
  - src/features/enrich/components/contact-batch-panel.tsx
  - src/features/retrieval-runs/components/retrieval-run-status-card.tsx
  - src/features/retrieval-runs/components/enriched-people-results.tsx
  - src/app/enrich/store/page.tsx
autonomous: true
requirements:
  - COST-05
  - EMAI-02
  - EMAI-04
must_haves:
  truths:
    - "Batch enrichment only sends globally unenriched members to Apollo."
    - "Users can enrich selected missing members or all missing members in a batch."
    - "Batch members show clear states such as not enriched, already enriched, unusable, and verified."
    - "The global enriched-store viewer and batch-scoped enriched results both reflect the full stored Apollo person payload."
  artifacts:
    - path: "src/features/enrich/components/contact-batch-panel.tsx"
      provides: "Enrich-selected and enrich-all-missing actions plus batch member state presentation"
    - path: "src/app/enrich/store/page.tsx"
      provides: "Global enriched-store viewer"
    - path: "src/features/retrieval-runs/components/enriched-people-results.tsx"
      provides: "Batch-scoped enriched results viewer"
  key_links:
    - from: "src/features/enrich/components/contact-batch-panel.tsx"
      to: "src/lib/retrieval/execution.ts"
      via: "kick off enrichment only for the batch subset missing from the central store"
      pattern: "kickoffRetrievalRun"
    - from: "src/app/enrich/store/page.tsx"
      to: "src/lib/db/repositories/enriched-people.ts"
      via: "show append-only global enrichment results"
      pattern: "listEnrichedPeople"
---

<objective>
Execute batch enrichment against only globally missing members and expose clear batch/global result views.

Purpose: Make contact batches operational while preserving the no-reenrichment rule and making results inspectable both per batch and globally.
Output: Batch enrich actions, batch-state display, batch/global results viewers, and integration with the global enriched-person cache.
</objective>

<context>
@.planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
@src/lib/retrieval/execution.ts
@src/features/retrieval-runs/components/retrieval-run-status-card.tsx
@src/features/retrieval-runs/components/enriched-people-results.tsx
@src/lib/db/repositories/enriched-people.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch enrich actions that target only globally missing members</name>
  <files>src/app/recipes/actions.ts, src/lib/retrieval/execution.ts, src/features/enrich/components/contact-batch-panel.tsx</files>
  <read_first>
    - src/lib/retrieval/execution.ts
    - src/features/retrieval-runs/components/retrieval-run-status-card.tsx
    - .planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
  </read_first>
  <action>Add enrich actions for selected batch members and all missing batch members. Before Apollo calls, resolve the target subset against the central enriched-people store and skip any member already present there, including prior unusable outcomes. Show clear batch-member status such as not enriched yet, already enriched, unusable, and verified. Reuse the existing retrieval executor as the backend enforcement point.</action>
  <verify>
    <automated>npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>The user can enrich selected or all missing members in a batch, and the system sends only globally missing Apollo person IDs to Apollo while showing clear per-member state.</acceptance_criteria>
  <done>Contact batches are operational and preserve the global no-reenrichment rule at execution time.</done>
</task>

<task type="auto">
  <name>Task 2: Expose batch-scoped and global enriched-result viewers</name>
  <files>src/features/retrieval-runs/components/enriched-people-results.tsx, src/app/enrich/store/page.tsx, src/features/enrich/components/contact-batch-panel.tsx</files>
  <read_first>
    - src/features/retrieval-runs/components/enriched-people-results.tsx
    - src/lib/db/repositories/enriched-people.ts
    - .planning/phases/04.1-contact-batch-enrichment-workflow/04.1-RESEARCH.md
  </read_first>
  <action>Show enriched results in two scopes: batch-scoped results in the contact batch workspace and the append-only global enriched store in `/enrich/store`. Use the shared snapshot viewer stack so the user can sort, filter, inspect, and export the full stored Apollo person payload in both views. Keep global records undeletable and keep batch membership removal separate from global storage.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>Users can inspect/export enriched results from a current batch and from the global enriched-person store separately, and those views expose the full stored Apollo `person` payload.</acceptance_criteria>
  <done>The enrichment workflow exposes both local batch results and the append-only global enrichment history clearly.</done>
</task>

</tasks>

<verification>
Run `npm run lint && npm run typecheck && npm run build`, then confirm enriching a batch skips already-covered people from the global store and that both batch and global enriched-result viewers render from the shared viewer stack.
</verification>

<success_criteria>
Contact batches can enrich only globally missing members, and the operator can inspect results both within the batch and in a separate global enriched-person store route.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-contact-batch-enrichment-workflow/04.1-contact-batch-enrichment-workflow-03-SUMMARY.md`
</output>
